You are reviewing my Body Bagz site’s HTML head and above-the-fold experience. 
Goal: perform a SAFE audit first, then propose MINIMAL, NON-BREAKING improvements as diffs. 
Do NOT refactor broadly. Do NOT apply changes automatically. First: analyze what already exists so we avoid duplicates or breaking behavior.

Context:
- Public site: https://body-bagz.replit.app
- I want you to focus on index.html HEAD tags (SEO/social/preview) and a lightweight ABOVE-THE-FOLD fallback hero ONLY IF React doesn’t already render an equivalent hero instantly.
- Real social links (no placeholders):
  - Telegram: https://t.me/BodyBagzs
  - X Community: https://twitter.com/i/communities/1960797896896602475

Current index.html (for reference):
- Title: "Body Bagz ($BAGZ) - Cyberpunk Crypto Token | Chaos Tools & Meme Generator"
- Meta description present
- OG/Twitter tags present using /og-image.jpg (relative path)
- JSON-LD present as WebApplication schema
- Canonical + robots present
- Fonts loaded from Google Fonts
- Root div + React mount

WHAT I WANT FROM YOU (order matters):

1) QUICK INVENTORY (no edits yet):
   Check index.html and confirm PRESENT/MISSING for:
   a) Favicons and touch icons: /favicon-32.png, /favicon-16.png, /apple-touch-icon.png
   b) OG enhancements: og:site_name, og:locale, and ABSOLUTE og:image URL
   c) Twitter account meta: twitter:site and twitter:creator (OK to keep temporary handle if unknown, but propose diff only if we can make it neutral or comment it)
   d) Performance hints: preconnect to origin; (optional) preload hero OG image
   e) Safer referrer policy: meta name="referrer" content="strict-origin-when-cross-origin"
   f) JSON-LD: presence of image, creator (Person: “Dr Frankenstein”), sameAs with Telegram + X Community
   g) Canonical link (already present—verify)
   h) Fallback hero section BELOW #root (ONLY if React doesn’t render a hero quickly). Must include real links: Telegram + X Community + optional White Paper (/whitepaper.pdf) but ONLY if that file exists.
   i) Assets existence: /og-image.jpg, favicons, apple icon (just note if missing; do not fabricate)

2) RISK ASSESSMENT:
   For any missing/partial items, label risk to break site as LOW/MED/HIGH and explain why. 
   Examples:
     - Adding head meta tags → LOW
     - Adding a static fallback hero (below #root) → LOW, but MUST be minimal and non-intrusive
     - Touching React entry → SKIP unless critical

3) PROPOSE MINIMAL CHANGES (DO NOT APPLY YET):
   Provide UNIFIED DIFFS (git-style) only for index.html (and only if needed). 
   Keep existing title/description as-is. 
   If an asset is referenced (e.g., /favicon-32.png) and doesn’t exist, include a commented TODO in the diff rather than breaking links.

   Target diffs (ONLY if missing/incomplete):
   A) Favicons/Touch icons (LOW risk):
      - <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png">
      - <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png">
      - <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
      - If files are absent, add the tags but annotate with <!-- TODO: add asset -->

   B) Open Graph enhancements (LOW risk):
      - <meta property="og:site_name" content="Body Bagz">
      - <meta property="og:locale" content="en_US">
      - Ensure og:image is ABSOLUTE: https://body-bagz.replit.app/og-image.jpg
      - Add og:image width/height (1200x630) if not present

   C) Twitter account tags (LOW risk):
      - If we do not have a final handle, either omit or include commented placeholders:
        <!-- <meta name="twitter:site" content="@YourHandle"> -->
        <!-- <meta name="twitter:creator" content="@YourHandle"> -->

   D) Small safety/perf nits (LOW risk):
      - <link rel="preconnect" href="https://body-bagz.replit.app" crossorigin>
      - <meta name="referrer" content="strict-origin-when-cross-origin">
      - Optional (ONLY if beneficial): <link rel="preload" as="image" href="/og-image.jpg"> with a comment noting file size should be ~400–600 KB

   E) JSON-LD tightening (LOW risk):
      - Keep @type = WebApplication
      - Ensure "image" points to absolute og image
      - Add "creator": { "@type": "Person", "name": "Dr Frankenstein" }
      - Add "sameAs": ["https://t.me/BodyBagzs","https://twitter.com/i/communities/1960797896896602475"]
      - Do not remove existing useful fields

   F) Fallback hero (LOW risk, ONLY IF React does not already render a hero quickly):
      - Inject a minimal <section id="hero-fallback"> AFTER the #root div
      - Must include 2 buttons with REAL links:
          Telegram → https://t.me/BodyBagzs
          X Community → https://twitter.com/i/communities/1960797896896602475
        And an OPTIONAL White Paper button ONLY if /whitepaper.pdf exists.
      - Style inline, minimal, and non-intrusive. 
      - Add an HTML comment: <!-- Remove if React renders hero immediately -->

4) SAFETY GUARANTEES:
   - Do NOT remove any existing tags unless strictly redundant/conflicting.
   - Do NOT change the title/description radically.
   - Do NOT add blocking scripts or heavy libraries.
   - Keep all additions minimal and reversible.
   - If unsure about an item’s existence (e.g., /whitepaper.pdf), add a TODO comment instead of a hard reference.

5) OUTPUT FORMAT:
   - Section A: Inventory table (Present/Missing/Unclear) with line references if possible.
   - Section B: Risk assessment with brief rationale.
   - Section C: Proposed UNIFIED DIFFS for index.html (only what’s needed).
   - Section D: Asset checklist (which files should exist: /og-image.jpg, favicons, apple icon, optional /whitepaper.pdf).
   - Section E: Post-change test checklist.

6) TEST CHECKLIST (include at the end):
   - Reload site: head has no duplicate/conflicting tags.
   - Share URL in X/Telegram: card shows title/description and og-image from the absolute URL.
   - Favicon shows in browser tab; apple icon not required to test now.
   - JSON-LD validates (structure only).
   - If fallback hero added: it appears before React loads and contains the correct links; once React hydrates with its own hero, the fallback can remain or be removed later.

Important:
- Some of these items MAY already exist via the React app. FIRST confirm presence; ONLY propose diffs for the missing pieces.
- Do NOT apply changes automatically — show me the diffs and I’ll approve.